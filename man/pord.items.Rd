% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pord.group.R
\name{pord.items}
\alias{pord.items}
\title{Apply the paired ordinal tests across many items}
\usage{
pord.items(X, Y, K = NULL, sign = FALSE, p.adjust.method = "holm", ...)
}
\arguments{
\item{X}{Matrix or data frame of \code{x} ratings, \strong{respondents in rows and
items in columns}.}

\item{Y}{Matrix or data frame of \code{y} ratings, same shape as \code{X}.}

\item{K}{Number of scale points.  Taken from the data when \code{NULL}.}

\item{sign}{Also run the sign test for \eqn{y < x} on each item.}

\item{p.adjust.method}{Method passed to \code{\link[stats:p.adjust]{stats::p.adjust()}}.}

\item{...}{Passed to \code{\link[=pord.test]{pord.test()}}, e.g. \code{method} or \code{B}.}
}
\value{
A data frame with one row per item: sample size, observed
proportion, the value expected under independence, the excess, the p-value
and its adjusted version, and -- when \code{sign = TRUE} -- the
corresponding sign-test columns.
}
\description{
\code{pord.items} runs \code{\link[=pord.test]{pord.test()}} (and optionally \code{\link[=pord.sign]{pord.sign()}}) on every
column of a pair of matrices, and adjusts for multiplicity across items.
}
\examples{
set.seed(1)
X <- matrix(sample(1:4, 60 * 4, TRUE, c(.05, .15, .35, .45)), 60, 4)
Y <- pmax(pmin(X - matrix(rbinom(60 * 4, 1, .4), 60, 4), 4), 1)
pord.items(X, Y, sign = TRUE)
}
\seealso{
\code{\link[=pord.test]{pord.test()}}, \code{\link[=pord.sign]{pord.sign()}}, \code{\link[=pord.group]{pord.group()}}
}
