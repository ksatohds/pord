% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pord.sign.R
\name{pord.compare}
\alias{pord.compare}
\title{Pairwise comparison of shortfall between domains of items}
\usage{
pord.compare(X, Y, domain, p.adjust.method = "holm")
}
\arguments{
\item{X}{Matrix or data frame of \code{x} ratings, \strong{respondents in rows and
items in columns}.}

\item{Y}{Matrix or data frame of \code{y} ratings, same shape as \code{X}.}

\item{domain}{Vector of length \code{ncol(X)} assigning each item to a
domain (subscale).  Note that this groups the \emph{columns}; in the
terminology of \code{\link[stats:friedman.test]{stats::friedman.test()}} the columns are its \code{groups}
and the respondents in the rows are its \code{blocks}.}

\item{p.adjust.method}{Method passed to \code{\link[stats:p.adjust]{stats::p.adjust()}}.}
}
\value{
A list of class \code{"pord.compare"} with the domain-level
proportions and mean ranks, the Friedman test and Kendall's W (defined as
\eqn{\chi^2 / (n (G - 1))}, so that it shares the tie correction applied
by \code{\link[stats:friedman.test]{stats::friedman.test()}}), and a data
frame of all pairwise sign tests.  Only respondents with complete data on
every item are used, since the comparison is within respondents.
}
\description{
\code{pord.compare} compares, within respondents, how often \eqn{y} falls
short of \eqn{x} between domains of items -- for example between functional
domains made up of different numbers of items.  Each respondent contributes
the proportion of items in a domain for which \eqn{y < x}; domains are then
compared by the sign test on the direction of the within-respondent
difference, so no distance between ordinal categories is used.

An overall Friedman test across domains, with Kendall's W, is reported
alongside the pairwise comparisons.
}
\examples{
set.seed(1)
X <- matrix(sample(1:4, 40 * 6, TRUE, c(.05, .15, .35, .45)), 40, 6)
Y <- pmax(pmin(X - matrix(rbinom(40 * 6, 1, .4), 40, 6), 4), 1)
pord.compare(X, Y, domain = rep(c("A", "B"), each = 3))
}
\seealso{
\code{\link[=pord.sign]{pord.sign()}}, \code{\link[=pord.items]{pord.items()}}
}
